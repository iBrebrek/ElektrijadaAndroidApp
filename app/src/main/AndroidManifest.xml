<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.fer.elektrijada" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.news.NewsFeedActivity" >
        </activity>
        <activity android:name=".activities.news.NewsActivity" >
        </activity>
        <activity android:name=".activities.news.EditNewsActivity" >
        </activity>
        <activity android:name=".activities.teams.TeamsActivity" >
        </activity>
        <activity android:name=".activities.teams.NewTeamActivity" >
        </activity>
        <activity android:name=".activities.competitions.CompetitionsActivity" >
        </activity>
    </application>

</manifest>
